const kalkulator = (x, y) => {
    let result = Number(x) + Number(y);
    return result;
}

const kalkulator2 = (x, y) => {
    let result = Number(x) + Number(y);
    return result;
}

const kalkulator3 = (x, y) => {
    let result = Number(x) + Number(y);
    return result;
}

const jsonSample = {
    "Color" : "Black",
    "Manufactur" : "BMW"
}

module.exports = {kalkulator, kalkulator2, kalkulator3, jsonSample}
